name: Test public ECR with lifecycle-policy raises error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a dummy lifecycle policy file
        run: |
          mkdir -p /tmp/ecr-test
          echo '{"rules":[]}' > /tmp/ecr-test/lifecycle.json

      - id: action
        uses: .
        continue-on-error: true
        with:
          public: 'true'
          repository: my-test-repo
          lifecycle-policy: /tmp/ecr-test/lifecycle.json

      - name: Assert action failed with expected error
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when public=true and lifecycle-policy is set"
            exit 1
          fi
          echo "PASS: action correctly failed"
